apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataeng-prod-0-oci-staging-7.fullname" . }}-test-connection"
  labels:
    {{- include "dataeng-prod-0-oci-staging-7.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataeng-prod-0-oci-staging-7.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
