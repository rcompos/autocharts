apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adas-prod-0-aws-cluster-0.fullname" . }}-test-connection"
  labels:
    {{- include "adas-prod-0-aws-cluster-0.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adas-prod-0-aws-cluster-0.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
