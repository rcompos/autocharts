apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adas-prod-0-gcluster-007-app.fullname" . }}-test-connection"
  labels:
    {{- include "adas-prod-0-gcluster-007-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adas-prod-0-gcluster-007-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
