apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataeng-dev-0-sergei-101-app.fullname" . }}-test-connection"
  labels:
    {{- include "dataeng-dev-0-sergei-101-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataeng-dev-0-sergei-101-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
