apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataeng-dev-0-cornholio-2022-aws.fullname" . }}-test-connection"
  labels:
    {{- include "dataeng-dev-0-cornholio-2022-aws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataeng-dev-0-cornholio-2022-aws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
