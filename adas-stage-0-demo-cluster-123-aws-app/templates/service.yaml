apiVersion: v1
kind: Service
metadata:
  name: {{ include "adas-stage-0-demo-cluster-123-aws-app.fullname" . }}
  labels:
    {{- include "adas-stage-0-demo-cluster-123-aws-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adas-stage-0-demo-cluster-123-aws-app.selectorLabels" . | nindent 4 }}
