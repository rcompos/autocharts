apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataeng-prod-0-lawrence-00-app.fullname" . }}-test-connection"
  labels:
    {{- include "dataeng-prod-0-lawrence-00-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataeng-prod-0-lawrence-00-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
